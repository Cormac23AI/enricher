# Copy this file to `.env` and fill in your key.
# The OpenAI key is OPTIONAL — it's only used as a last-resort fallback
# to extract obfuscated emails (e.g. "info [at] domain [dot] com").
# Leave it blank and the scraper will simply skip the GPT step.

OPENAI_API_KEY=
